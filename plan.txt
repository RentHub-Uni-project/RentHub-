plan:
    - auth:
        * login (all)
        * register (anonymos)
        * logout (all)

    - users:
        * update profile (all)
        * get profile (all)
        * delete profile (all)

        * create user (admin)
        * update user (admin)
        * delete user (admin)
        * list users (admin)
        * get user (admin)

    - apartments
        * create apartment (owner)
        * update my apartment (owner)
        * delete my apartment (owner)
        * list my apartments (owner)

        * list apartments (all)
        * get apartment (all)

        * create, update, delete, list, get (admin)

    - apartment_images (embeded with apartments' table controllers)

    - favorite_apartments
        * add apartment to favorites (tenant)
        * remove apartment from favorites (tenant)
        * get apartment from favorites (tenant)         x
        * list my favorite apartments (tenant)

        * create, delete, list, get (admin)


    - reviews
        * create apartment review (tenant)
        * update apartment review (tenant)
        * delete apartment review (tenant)
        * list my reviews (tenant)

        * list apartment reviews (all)

        * create, update, delete, list (admin)

    - bookings
        * create booking (tenant)
        * delete booking (tenant)
        * update booking (tenant)
        * get booking (tenant)
        * change booking status (owner)
        * list my apartments' bookings (owner)

        * create, update, delete, list, get (admin)

    - booking_update_requests
        * create booking update request (tenant)
        * delete booking update request (tenant)
        * list my booking update requests (tenant)
        * get booking update request (tenant, owner)
        * change booking update request status (owner)
        * list my apartments' bookings' update requests (owner)

        * create, update, list, get, delete (admin)

    - payments
    - conversaions
    - messages
    - notifications


commands:
    build:
        composer install
        cp .env.example .env
        php artisan key:generate
        php artisan storage:link
        php artisan migrate

    run:
        php artisan serve


for amjad:
    - one list endpoint for apartments with filtering
    - apply filtering on adminIndex function in ApartmentController
    - what amentities means in AdminApartmentRequest?
    - make partial updates in AdminApartmentRequest for update controller
